require "spec_helper"

RSpec.describe Saml::Simplified do
  it "has a version number" do
    expect(Saml::Simplified::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
